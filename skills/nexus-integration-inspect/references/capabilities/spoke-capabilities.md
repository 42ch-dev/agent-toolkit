# Spoke 能力基线（Capability Baseline）

| 元数据 | 值 |
|--------|----|
| 产品 | SPOKE（Standardized Programmable Ontology Knowledge Engine；JSON Schema wire contract 协议仓库，非 runtime） |
| 调研日期（survey_date / research_date） | 2026-08-15 |
| surveyed HEAD SHA（full） | `05915ad68149d6f998bb637fe8905de4edbfc9ef` |
| surveyed HEAD SHA（short） | `05915ad` |
| HEAD 提交 | `chore(release): bump version to 0.10.0 (#81)` |
| 调研范围 | 只读；`../spoke`（crates/Package.swift/go.mod/CHANGELOG.md/docs/STRATEGY.md/CONCEPTS.md），README*/docs/STRATEGY/CONCEPTS/CHANGELOG、manifest 版本号、公开 API 面（库/包/绑定/协议信封）；未做逐 crate 源码盘点 |

**版本来源优先级**：`Cargo.toml [workspace.package] version`（workspace manifest，本仓库为 `0.10.0`，全部 crates 经 `version.workspace = true` 继承）> `CHANGELOG.md` 最新条目（`[0.10.0] - 2026-08-14`，与 workspace 版本一致）> README/docs 声明版本。多 manifest 仓库（Cargo.toml + Package.swift + go.mod）：npm 消费面取 `packages/<pkg>/package.json`；`Package.swift`（SPM 二进制 target）与 `go.mod`（仅 module 路径 + go 版本）**不含版本号**，相关能力回退到 workspace manifest。所有发布面 lockstep 同一 SemVer `0.10.0`，git tag `vX.Y.Z`。

## 能力表

| id | product | name | description | integration surface | version | version_source | survey_date | research_date | source_path |
|----|---------|------|-------------|---------------------|---------|----------------|-------------|---------------|-------------|
| spoke.schemas.data | Spoke | 数据层 wire schemas | 九种持久数据对象契约：KnowledgeEntry、Relation、SourceAnchor、Finding、Rule、TimelineEvent、MindState（l5-mind）、HostCapabilityManifest、AssemblePacket；`additionalProperties: false` 封闭信封 + 开放 `entry_type` 词表 | JSON Schema（Draft-07，`schemas/data/`，SSOT） | 0.10.0 | `../spoke/Cargo.toml [workspace.package] version` | 2026-08-15 | 2026-08-15 | ../spoke/schemas/data/（另见 README.md §Core concepts、CONCEPTS.md） |
| spoke.schemas.ops | Spoke | ops wire 信封 | 传输无关请求/响应族：baseline `upsert` / `promote`（extract→promote）/ `relate` / `check` / `assemble`，可选 `project` / `compute`（l2-computable）；共用 `Scope` 选择器与 `ErrorEnvelope` | JSON Schema（`schemas/ops/`，SSOT） | 0.10.0 | `../spoke/Cargo.toml [workspace.package] version` | 2026-08-15 | 2026-08-15 | ../spoke/schemas/ops/（另见 README.md §Operations） |
| spoke.schemas.connect | Spoke | Connect 信封族 wire schemas | 六个 opt-in 交互信封：ConnectHello（签名 manifest 交换）、ConnectSession、ConnectInvokeRequest/Response（把现有 ops 信封作不透明 `payload` 远程调用）、ConnectAuthChallenge/Response（可扩展 `method` 认证） | JSON Schema（`schemas/connect/`，SSOT） | 0.10.0 | `../spoke/Cargo.toml [workspace.package] version` | 2026-08-15 | 2026-08-15 | ../spoke/schemas/connect/（另见 CONCEPTS.md §Connect envelope family） |
| spoke.sdk.typescript | Spoke | @42ch/spoke-schemas + @42ch/spoke-operations（npm） | 生成的 TS wire 类型 + 手写生命周期助手（纯函数、port 契约、orchestrate* 注入编排）；与 Rust 侧 lockstep 同版本 | SDK（npm，TypeScript） | 0.10.0 | `../spoke/packages/spoke-schemas/package.json version`（spoke-operations 同值；两包须 pin 同一 SemVer） | 2026-08-15 | 2026-08-15 | ../spoke/packages/spoke-schemas/package.json（另见 packages/spoke-operations/package.json、README.md §Install） |
| spoke.sdk.rust | Spoke | spoke-schemas + spoke-operations（crates.io） | 生成的 Rust wire 类型 + 生命周期助手/port trait/orchestrate_*（re-export spoke_schemas），与 TS 侧 parity | SDK（crates.io，Rust crate） | 0.10.0 | `../spoke/Cargo.toml [workspace.package] version`（crates/spoke-schemas/Cargo.toml `version.workspace = true`） | 2026-08-15 | 2026-08-15 | ../spoke/crates/spoke-schemas/（另见 crates/spoke-operations/、README.md §Rust） |
| spoke.sdk.connect | Spoke | Connect 客户端（TS + Rust reference） | `@42ch/spoke-connect`（npm，语言原生 session core + WebSocket 传输，Path A）与 `spoke-connect`（crates.io，libp2p 传输 + uniffi 绑定源，Path B 共享核心 reference） | SDK（npm + crates.io，opt-in 声明） | 0.10.0 | `../spoke/packages/spoke-connect/package.json version`（crates/spoke-connect 经 workspace 同值） | 2026-08-15 | 2026-08-15 | ../spoke/packages/spoke-connect/package.json（另见 crates/spoke-connect/、README.md §Connect） |
| spoke.sdk.native-bindings | Spoke | 原生语言绑定（FFI 发布通道） | spoke-connect 共享 session core 的 Path B 绑定，覆盖 C#（NuGet）、Kotlin（Maven）、Swift（SPM，见 `Package.swift` 的 `SpokeConnect` library）、Go（Go modules）、Python（PyPI） | SDK/FFI（五个发布通道；绑定源在 crates/spoke-connect/bindings/） | 0.10.0 | `../spoke/Cargo.toml [workspace.package] version`（绑定无独立 manifest 版本；`Package.swift`/`go.mod` 不含版本号，回退 workspace） | 2026-08-15 | 2026-08-15 | ../spoke/crates/spoke-connect/bindings/（另见 Package.swift、go.mod、README.md §Connect） |
| spoke.sdk.toy-world | Spoke | ToyWorldAdapter 参考实现与一致性 fixtures | 协议一致性夹具 + 可运行参考适配器：`fixtures/toy-world/`（TS `ToyWorldAdapter` + Rust crate `spoke-fixture-toy-world`，in-memory OCC Baseline + Full 桩），含"米拉在海港"示例图 | SDK/参考实现（fixtures + AJV/Vitest harness） | 0.10.0 | `../spoke/Cargo.toml [workspace.package] version`（fixtures/toy-world/rust/Cargo.toml `version.workspace = true`） | 2026-08-15 | 2026-08-15 | ../spoke/fixtures/toy-world/（另见 README.md §Quick start） |
| spoke.agent-api.operations | Spoke | 适配器 port + orchestrate 注入编排 | 能力切片 port 契约（KnowledgeEntryPort/RelationPort/FindingPort/ScopeQueryPort/RuleQueryPort/HostManifestPort/Timeline 查询等）与编排入口：`orchestrateUpsert/Promote/Relate/Check/Assemble` + 可选 `Project/Compute/ForkCheck/ForkAssemble`；组合别名 BaselineAdapter/ComputableAdapter/ForkAdapter/FullAdapter；统一 `SpokeResult`/`SpokeRejectCode` | 库接口（TS + Rust parity；agent/产品适配器调用面） | 0.10.0 | `../spoke/Cargo.toml [workspace.package] version`（另见 CHANGELOG.md `[0.10.0]`） | 2026-08-15 | 2026-08-15 | ../spoke/README.md §Operations（另见 CONCEPTS.md §HostManifestPort） |
| spoke.agent-api.connect | Spoke | spoke-connect 协议能力（跨进程交互） | 声明 `spoke-connect` 即实现：签名 hello 握手、按 session 单调 `sequence` 排序 + `request_id` 关联、`noise-peerid` 授权（部署配置 allowlist）、能力令牌（RFC 8785 JCS + Ed25519 签名，离线验证）；非 `spoke-baseline` | Agent/协议接口（跨进程会话；双 host 声明时协商进 negotiated_capabilities） | 0.10.0 | `../spoke/Cargo.toml [workspace.package] version`（另见 CHANGELOG.md `[0.10.0]` connect 修复） | 2026-08-15 | 2026-08-15 | ../spoke/CONCEPTS.md §spoke-connect / §Session (connect) / §capability token（另见 README.md §Optional capabilities） |
| spoke.data.domain-profiles | Spoke | Domain Profile 词汇发布与 modules.* 方言 | 不封闭核心 enum 的词汇发布机制：开放 `entry_type` 字符串 + 适配器规格词表 + 可选 `modules`（ModuleMap）功能方言袋（narrative-modules：lore activation / placement / activation_trace / mental / belief / observation）；扩展仍走 `extensions.<namespace>`；适配器须 verbatim round-trip 未知命名空间 | 协议/数据面（handbook 定义方言内形状；`extensions.<namespace>` 产品私有） | 0.10.0 | `../spoke/Cargo.toml [workspace.package] version` | 2026-08-15 | 2026-08-15 | ../spoke/CONCEPTS.md §Domain Profile / §Modules / §Extensions（另见 docs/explanation/domain-profiles） |

## 说明

- `survey_date` 与 `research_date` 同义（brief 字段名 = contract `survey_date` 的别名），本文件两列同值 `2026-08-15`。
- 全部发布面 lockstep `0.10.0`（workspace manifest、npm ×3、CHANGELOG `[0.10.0] - 2026-08-14`、git tag `v0.10.0` 同源）；`Package.swift` 与 `go.mod` 无版本号字段，相关能力回退 workspace manifest 并在 `version_source` 记录。
- 协议能力开关（`spoke-baseline` / `l2-computable` / `l5-fork` / `l5-mind` / `narrative-modules` / `spoke-connect`）见 README.md §Optional capabilities 与 CONCEPTS.md；本基线将能力落为行，flag 语义写进各行 description。
- 刷新与漂移检测：见同目录 README.md §刷新指引。
